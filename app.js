const player = {
    name : "Gyejin",
    points : 10,
    fat : false,
};

console.log(player);

// update value
player.points += 15;

// add something in object
player.lastName = "Cho";

console.log(player);